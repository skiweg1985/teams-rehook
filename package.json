{
  "name": "teams-rehook",
  "version": "0.1.0",
  "description": "Teams webhook relay for forwarding operational messages into Microsoft Teams",
  "private": true,
  "type": "module",
  "scripts": {
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "backend:check": "python3 -m py_compile backend/app/*.py backend/app/routers/*.py backend/app/core/*.py backend/app/services/*.py",
    "backend:test": "python3 -m pytest",
    "test": "npm run frontend:build && npm run backend:check && npm run backend:test"
  }
}
